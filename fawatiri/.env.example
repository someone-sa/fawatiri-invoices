# Copy this file to .env only if you have actual local configuration values to set.
# Roles (owner/receiver) are managed in Firestore -> config/roles, NOT here.
# The .env file is gitignored so it stays out of the public repo.