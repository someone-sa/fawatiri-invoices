name: fawatiri
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # --- المكتبات الأساسية (موجودة) ---
  cupertino_icons: ^1.0.8

  # --- المكتبات الجديدة (أضفها هنا) ---
  flutter_riverpod: ^2.5.1
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.1.0
  go_router: ^14.0.0
  uuid: ^4.4.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  collection: ^1.18.0  # ← أضف هذا
  crypto: ^3.0.3
  flutter_dotenv: ^6.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env